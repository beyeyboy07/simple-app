[phases.setup]
nixPkgs = ["php85", "php85Extensions.gd"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-interaction"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"